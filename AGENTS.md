# AGENTS.md

## Testing Requirements
All changes must pass `python test_environment.py` before committing.

## Secrets Policy
Do not include API keys, passwords, or sensitive data in commits or prompts.

## Scope Boundaries
Agents may edit files in `src/` and `notebooks/`.
Do not modify `requirements.txt` without human review.

## Reproducibility Standard
All changes must be tested locally before committing.